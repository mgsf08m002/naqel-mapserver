services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  pgadmin:
    ports:
      - "127.0.0.1:5050:80"

  geotrak_maps:
    ports:
      - "8000:8000"
    volumes:
      - ./database:/database
    restart: unless-stopped
