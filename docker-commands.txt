#Remove Everything
docker compose down --volumes --remove-orphans --rmi all

#Build From scratch
docker compose build --no-cache

#Start containers
docker compose up -d
docker compose up --build (for logs)