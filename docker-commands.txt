# Docker commands for naqel-mapserver
# Sample GeoJSON data is in docs/sample-geojson.json

# Remove everything (containers, volumes, images)
docker compose down --volumes --remove-orphans --rmi all

# Build from scratch
docker compose build --no-cache

# Start containers (detached)
docker compose up -d

# Start with build and view logs
docker compose up --build


ALTER TABLE public.riyadh_roads
ADD COLUMN road_closure integer NOT NULL DEFAULT 0;